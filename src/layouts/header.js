import React, { Component } from "react";

export class Header extends Component {
  render() {
    return (
      <div className="container">
        <h1>Welcome to React with Redux Project</h1>
      </div>
    );
  }
}

export default Header;
